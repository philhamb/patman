 <tr>
 <td><%= treatment.created_at.strftime("%e/%_m/%Y ") %> </td>
 <td><%= treatment.notes %> </td>
 <td><%= treatment.tests %></td>
 <td><%= treatment.treatment %></td>
 <td><%= link_to "Edit", (edit_patient_treatment_url(@patient, treatment.id )) %></td>
 </tr>
  
 
