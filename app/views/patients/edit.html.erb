
<h1>Update <%= @patient.f_name + " " + @patient.s_name %></h1>
 
    <%= form_for(@patient) do |f| %>
    <%= render 'fields', :f => f %>
    <div class="actions">
      <%= f.submit "Update" %>
    </div>
    <% end %>
    
   </div> <!-- closes blueprint span tag -->
   

